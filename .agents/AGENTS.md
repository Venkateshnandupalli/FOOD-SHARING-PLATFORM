# SharePlate AI Project Rules

## Version Control
Always commit and push any changes made in this project to GitHub. 
Whenever you complete a task or a set of modifications, run the following commands automatically to keep the remote repository updated:
```powershell
git add .
git commit -m "feat: <describe changes>"
git push -u origin main
```
